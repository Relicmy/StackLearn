📝 Git + GitHub — Шпаргалка для начинающего (и не только)
🔧 1. Установка и настройка (делается 1 раз)
баш


# Установи Git: pip freeze > requirements.txt
# Проверь версию
git --version / git -v

# Настрой имя и email (обязательно!)
git config --global user.name "Твоё Имя"
git config --global user.email "твоя@почта.com"
💡 Электронная почта должна соответствовать темам, которые есть в GitHub (иначе коммиты не привяжутся к профилю). 

🌐 2. Настройка SSH (рекомендуется)
баш

# Создай ключ (нажимай Enter везде)
ssh-keygen -t ed25519 -C "твоя@почта.com"

# Запусти агент и добавь ключ
eval $(ssh-agent -s)
ssh-add ~/.ssh/id_ed25519

# Скопируй публичный ключ
cat ~/.ssh/id_ed25519.pub


баш


# Проверь подключение
ssh -T git@github.com
# Должно быть: "Hi Relicmy! You've successfully authenticated..."



📁 3. Первый проект: с нуля
Вариант А: Код на компьютере уже есть.
баш


cd путь/к/проекту
git init
git add .
git commit -m "Initial commit"
git remote add origin git@github.com:Relicmy/название-репо.git
git push -u origin main

Вариант Б: Создасщтвф


git clone git@github.com:Relicmy/название-репо.git
cd название-репо

# добавляй файлы, коммить, пушь
🔄 4. Ежедневная работа (основной цикл)
баш

# 1. Посмотри изменения
git status

# 2. Добавь всё (или конкретные файлы)
git add .
# или
git add файл.py

# 3. Сделай коммит
git commit -m "Краткое описание"

# 4. Отправь на GitHub
git push

💡 После первого всегда использую просто .git push -u origin maingit push 

🌿 5. Работа с ветками
баш


# Посмотреть ветки
git branch

# Создать и перейти в новую ветку
git checkout -b feature/новая-функция

# Работай → коммить → пуш
git add .
git commit -m "Добавил функцию"
git push -u origin feature/новая-функция

# Вернуться в main
git checkout main

# Обновить main с GitHub
git pull origin main

# Удалить локальную ветку (после мёрджа)
git branch -d feature/новая-функция

# Посмотреть ветки
git branch

# Создать и перейти в новую ветку
git checkout -b feature/новая-функция

# Работай → коммить → пуш
git add .
git commit -m "Добавил функцию"
git push -u origin feature/новая-функция

# Вернуться в main
git checkout main

# Обновить main с GitHub
git pull origin main

# Удалить локальную ветку (после мёрджа)
git branch -d feature/новая-функция



📤 6. Запрос на включение (PR) — безопасный способ слияния.
На GitHub: Запросы на извлечение → Новый запрос на извлечение
Выбери:
base: main
compare: твоя-ветка
Напиши описание → Создать пул-реквест
Нажми Merge pull request → Confirm
✅ Так ты не сломаешь !main 

🔒 7. Защита основной ветки (главная)
Даже на бесплатном GitHub!

В репозитории: Настройки → Ветки
Add rule → введиmain
Поставь галочки:
✅ Защитите эту ветку
✅ Требуйте запрос на извлечение перед слиянием
✅ Включить администраторов
Создавать
→ Теперь никто не может пушить напрямую в !main

👥 8. Совместная работа (с братом, другом, командой)
Вариант А: Через соавторов (проще)
Настройки → Соавторы → Добавить людей
Дай ему Написать доступ
Он клонирует репо, создаёт ветки, делает пиар → ты мёржишь
Вариант Б: Через форк (безопаснее, для открытого исходного кода)
Он нажимает вилку на твое репо
Работает у себя → делает пиар из своей вилки в твоюmain
Ты проверяешь и мёржишь

# Посмотреть историю
git log --oneline
# Посмотреть
git diff
# Отменить последний коммит (если не пушнул)
git commit --amend -m "Новое сообщение"
# Отменить изменения в файле
git checkout -- файл.py
# Обновить локальное репо
git pull origin main
# Удалить ветку на GitHub
git push origin --delete имя-ветки
# Посмотреть удалённые ветки
git branch -r

🛠 9. Полезные команды
Посмотреть историю
git log --oneline
Посмотреть
git diff
Отменить последний коммит (если не пушнул)
git commit --amend -m "Новое сообщение"
Отменить изменения в файле
git checkout -- файл.py
Обновить локальное репо
git pull origin main
Удалить ветку на GitHub
git push origin --delete имя-ветки
Посмотреть удалённые ветки
git branch -r

❗10 . Распространённые ошибки и решения

# fatal: remote origin already exists - git remote remove origin → git remote add origin ...
# rejected: fetch first - git pull origin main --allow-unrelated-histories
# Permission denied (publickey) - Проверь SSH-ключ → ssh -T git@github.com
# Не видит файлы - Проверь → .gitignore cat .gitignore
# Хочу отменить всё - git reset --hard HEAD (осторожно!)

💡 Советы на будущее
Всегда работай в ветках , а не в .main
Делайте маленькие коммиты с понятными сообщениями.
Используй Pull Request — даже в одиночку.
Регулярно делайте , чтобы не отставать .git pullmain
Не сообщать секреты (пароли, токены) — используйте + ..env.gitignore
🎁 Бонус: для Python (пример).gitignore
Создай файл в корне проекта:.gitignore

gitignore

__pycache__/
*.pyc
.env
venv/
.env.local
.DS_Store
*.log
.ipynb_checkpoints/