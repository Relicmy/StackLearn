 - name-var: $AUT
   sintax: SIGNAL $AUT $OUT[номер]
   discriptions: |
    сигнал режима Автоматической работы
    📌 Назначение:
    $AUT — это системный выходной сигнал, который автоматически устанавливается в TRUE,
    когда контроллер KUKA находится в режиме AUTOMATIC (автоматический режим, без участия оператора).
    💡 Это не переменная, которую вы меняете вручную — это сигнал, привязанный к физическому или виртуальному выходу ($OUT),
    и он управляется системой.
    По умолчанию: $OUT[995]
    → Но вы можете переназначить его в машинных данных (Machine Data), если нужно.
    Тип: INT (номер выхода)
    🔧 Как это работает?
    Когда вы переключаете пульт KUKA в AUT (автоматический режим), система автоматически активирует указанный выход (например, $OUT[995] := TRUE).
    В режиме T1/T2 (ручное управление) — сигнал снят (FALSE).
    💡 Практическое применение:
    Индикация режима на пульте оператора или ПЛК:
    → ПЛК может проверять этот сигнал, чтобы разрешить/запретить запуск линии.
    Безопасность:
    → Внешние устройства (например, конвейер) запускаются только если $AUT = TRUE.

   example: |
    IF $OUT[995] == TRUE THEN
    ; выполняем только в автомате
    ENDIF

    ⚠️ Но: в KRL обычно не читают $OUT как вход — лучше использовать системный флаг режима, например:
    IF $MODE_OP == #AUT THEN ...
    Это надёжнее, чем проверка $AUT через $OUT. 
