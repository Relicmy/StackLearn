 - name-var: $COM_VAL_MI
   sintax: |
     $COM_VAL_MI[AxisNumber] = LimitValue
   discriptions: |
     Ограничение командной скорости осей
     🔹 Назначение
     Ограничивает максимально допустимую скорость для каждой оси в процентах от её номинального значения.

     Это командное ограничение, а не физическое — влияет на расчёт траектории, а не на экстренное торможение.
     |AxisNumber | Номер оси: 1–6 → A1–A6, 7–12 → E1–E6|
     |LimitValue | Тип: REAL, единица: %, по умолчанию 150.0
     🔹 Важно!
     ⚠️ Документация прямо говорит:
     "This value must not be changed."
     → То есть менять нельзя в стандартных условиях. 
     Значение по умолчанию 150% — позволяет контроллеру использовать ускорения выше 100% при оптимизации траектории (например, для плавности), но итоговая скорость всё равно ограничена другими параметрами ($VEL_AXIS, $VEL_CP и т.д.).
     Изменение может нарушить калибровку, безопасность или динамические расчёты.
     💡 Вывод: это системная настройка, предназначенная для внутреннего использования KSS. Не используйте в пользовательских программах. 
   example: |
     $COM_VAL_MI[AxisNumber] = LimitValue

 - name-var: $CONF_MESS
   sintax: |
     SIGNAL $CONF_MESS $IN[1026]
   discriptions: |
     По умолчанию привязан к входу 1026.
     Обрабатывается только фронт (rising edge) — удержание сигнала не нужно.
     🔹 Условия работы
     Работает только если $I_O_ACT == TRUE (активен интерфейс Automatic External).
     Не подтверждает аварийные остановы (например, EMERGENCY STOP) — только технологические ошибки, которые можно сбросить программно.
     💡 Пример: робот остановился из-за "перегрева инструмента" → ПЛК охладил инструмент → подал импульс на $CONF_MESS → робот продолжает программу. 
   example: |
     SIGNAL $CONF_MESS $IN[1026]

 - name-var: $COULD_START_MOTION
   sintax: |
     SIGNAL $CONF_MESS $IN[1026]
   discriptions: |
     Можно ли запустить движение?
     🔹 Назначение
     Индикатор готовности к движению: показывает, возможно ли сейчас запустить программу или джоггинг.

     Это итоговый результат проверки всех условий безопасности и состояния системы. 

     🔹 Тип и значение
     Тип: BOOL (только для чтения)
     TRUE → робот может двигаться
     FALSE → движение заблокировано
     🔹 Что проверяется?
     Активные ошибки или предупреждения, блокирующие движение.
     Сигналы безопасности:
     $MOVE_ENABLE (разрешение движения от ПЛК),
     Safety Stop (останов безопасности),
     Operator Safety (кнопка «Enable» у оператора),
     Разрешение на джоггинг и т.д.
   example: |
    IF $COULD_START_MOTION == FALSE THEN
      MsgNotify("Движение невозможно: проверьте безопасность и ошибки!")
    ENDIF
