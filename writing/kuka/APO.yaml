 - name-var: $APO
   sintax: |
    $APO = {CVEL: ..., CPTP: ..., CDIS: ..., CORI: ...}
   discriptions: |
     В KUKA робот может не останавливаться точно в каждой промежуточной точке траектории, а «срезать угол» — двигаться плавно, не теряя скорости.
     Это называется аппроксимация (approximation).
     Параметры аппроксимации задаются через переменную $APO.
     Каждое поле определяет условие, при котором начинается приближённое (аппроксимированное) движение к следующей точке.
     Робот начнёт «срезать путь», как только будет выполнено хотя бы одно из этих условий.
     📚 Разбор полей:
     1. CVEL — скоростной критерий
     Тип: INT
     Диапазон: 1 … 100
     Единица: проценты (%)
     Определяет, на каком проценте от запрограммированной скорости робот может начать аппроксимацию при торможении к конечной точке. 

     Например:

     CVEL = 50 → как только текущая скорость упадёт до 50% от целевой, робот может начать сглаживать траекторию к следующей точке.
     ⚠️ Применяется только для CP-движений (непрерывных по траектории, например LIN, CIRC). 
      2. CPTP — аппроксимация для PTP-движений
      Тип: INT
      Диапазон: 1 … 100
      Единица: проценты (%)
      Задаёт максимальное расстояние (в % от всего пути), на котором можно начать аппроксимацию в PTP- или SPTP-движениях. 

      Например:

      CPTP = 10 → робот может начать сглаживать движение, когда до конечной точки останется не более 10% пути.
      💡 Это не миллиметры, а проценты от угла поворота каждой оси в PTP-перемещении. 

      3. CDIS — линейное расстояние до цели
      Тип: REAL
      Единица: миллиметры (мм)
      Аппроксимация начинается, когда линейное расстояние от текущего положения фланца до целевой точки становится меньше указанного значения. 

      Например:

      CDIS = 5.0 → как только робот подойдёт ближе чем на 5 мм к точке, он может начать сглаживать движение к следующей.
      ✅ Применяется для CP-движений (LIN, CIRC). 

      4. CORI — угловое отклонение ориентации
      Тип: REAL
      Единица: градусы (°)
      Аппроксимация разрешается, когда угол поворота инструмента (относительно целевой ориентации) становится меньше указанного значения. 

      Например:

      CORI = 2.0 → если ориентация инструмента отличается от цели менее чем на 2°, можно начинать сглаживание.
      ✅ Используется для CP-движений с изменением ориентации. 

      💡 Как это работает на практике?
      Робот постоянно проверяет все четыре условия. Как только хотя бы одно из них выполняется — он переходит к следующей точке без полной остановки, плавно сглаживая траекторию.

      Это позволяет увеличить скорость выполнения программы и снизить износ за счёт отсутствия резких остановок. 
   example: |
    $APO.CDIS = 10.0   ; начинать аппроксимацию за 10 мм до точки
    $APO.CORI = 5.0    ; или если ориентация близка в пределах 5°
    $APO.CVEL = 30     ; или когда скорость упала до 30% от максимума
    ; для PTP:
    $APO.CPTP = 20     ; начинать сглаживание за последние 20% пути
    DEF APO_ON()
      $APO.CDIS = 10.0
      $APO.CORI = 5.0
      $APO.CVEL = 30
      $APO.CPTP = 20
    END

    DEF APO_OFF()
      $APO.CDIS = 0.0
      $APO.CORI = 0.0
      $APO.CVEL = 0
      $APO.CPTP = 0
    END


 - name-var: $APO.CPTP
   sintax: $APO.CPTP = 20  ; INT, 20% — корректно
   discriptions: |
     это параметр аппроксимации для PTP- и SPTP-движений (точка-в-точку и сплайновые PTP).
     Он всегда:
     Тип: INT
     Диапазон: 1 … 100
     Единица измерения: проценты (%)
     📌 Как работает CPTP?
     1. Для обычного PTP-движения:
     Значение CPTP задаёт процент от максимального угла поворота любой оси между начальной и конечной точкой.
     Фактическое угловое расстояние (в градусах) определяется через параметр машины $APO_DIS_PTP (машинные данные).
     Но есть важное ограничение: аппроксимация не начнётся раньше, чем пройдено 50% пути (по времени или по углу).
     → Это мера безопасности, чтобы избежать слишком раннего "срезания".
     Пример: 

     PTP из точки A в B: ось 1 поворачивается на 90°.
     CPTP = 10 → робот может начать сглаживать движение, когда до конца останется ≤ 9° (10% от 90°).
     Но только после того, как уже пройдено ≥ 45° (50% пути).

   example: |
        ; Настроить аппроксимацию для PTP
        $APO.CPTP = 15   ; начинать сглаживание за последние 15% угла поворота
        PTP P1
        PTP P2           ; между P1 и P2 — плавный переход без полной остановки

        ; Если нужно точное позиционирование то $APO.CPTP = 0


 - name-var: $APO_C
   sintax: $APO_C.CDIS где CDIS это одно из значений структуры
   discriptions: |
    это только для чтения:
    Содержит текущие параметры аппроксимации, которые система реально использует в данный момент.
    Нельзя изменять (write-protected).
    Полезно для отладки: можно вывести в HMI или лог.
     Параметр	 Тип	Ед. изм.	Применяется к	Назначение
     CPTP	     INT	%	        PTP / SPTP	% угла/пути до конца, при котором разрешено сглаживание
     CDIS	     REAL	мм	      LIN, CIRC	Линейное расстояние до точки
     CORI	     REAL	°	        LIN, CIRC	Отклонение ориентации
     CVEL	     INT	%	        CP-движения	Скорость, при которой разрешено сглаживание
   example: |
    ; Убедимся, что аппроксимация отключена перед точной операцией
    IF ($APO_C.CDIS <> 0.0) OR ($APO_C.CORI <> 0.0) THEN
    ; Можно выдать предупреждение или принудительно сбросить
    ; (но сброс делается через $APO, не через $APO_C!)
    $APO.CDIS = 0.0
    $APO.CORI = 0.0
    ; После этого $APO_C обновится автоматически
    ENDIF