 - name-var: $BIN_IN
   sintax: >
    $BIN_IN[3] = {F_BIT 3, LEN 5, PARITY #EVEN}
   discriptions: |
    🛠 Настройка:
      в $CUSTOM.DAT:
      $BIN_IN[2] = {F_BIT 0, LEN 8, PARITY #NONE}

    | Поле   | Значение | Что значит |
    |--------|----------|------------|
    | `F_BIT`  | 3        | Первый бит — бит №3 (нумерация зависит от платы) |
    | `LEN`    | 5        | Используется 5 битов (биты 3–7) |
    | `PARITY` | `#EVEN`  | Добавляется бит чётности, чтобы общее число единиц было чётным |

    **Пример распределения битов (младший бит → первый выход):**

    | Бит (2ⁿ)     | 2⁰ | 2¹ | 2² | 2³ | 2⁴ |
    |--------------|----|----|----|----|----|
    | **Значение** | 1  | 2  | 4  | 8  | 16 |
    | **Выход**    | `$OUT[10]` | `$OUT[11]` | `$OUT[12]` | `$OUT[13]` | `$OUT[14]` |
    | **Значение для 7** | 1 | 1 | 1 | 0 | 0 |
   example: |
    $BIN_IN[1] = {F_BIT 10, LEN 8, PARITY #NONE}


 - name-var: $BIN_OUT
   sintax: >
    $BIN_OUT[3] = {F_BIT 3, LEN 5, PARITY #EVEN}
   discriptions: |
    🛠 Настройка:
      в $CUSTOM.DAT:
      $BIN_OUT[2] = {F_BIT 0, LEN 8, PARITY #NONE}

    | Поле   | Значение | Что значит |
    |--------|----------|------------|
    | `F_BIT`  | 3        | Первый бит — бит №3 (нумерация зависит от платы) |
    | `LEN`    | 5        | Используется 5 битов (биты 3–7) |
    | `PARITY` | `#EVEN`  | Добавляется бит чётности, чтобы общее число единиц было чётным |

    **Пример распределения битов (младший бит → первый выход):**

    | Бит (2ⁿ)     | 2⁰ | 2¹ | 2² | 2³ | 2⁴ |
    |--------------|----|----|----|----|----|
    | **Значение** | 1  | 2  | 4  | 8  | 16 |
    | **Выход**    | `$OUT[10]` | `$OUT[11]` | `$OUT[12]` | `$OUT[13]` | `$OUT[14]` |
    | **Значение для 7** | 1 | 1 | 1 | 0 | 0 |
   example: |
    $BIN_OUT[1] = {F_BIT 10, LEN 8, PARITY #NONE}
 
 - name-var: $B_OUT
   syntax: $B_OUT[номер] = значение
   discriptions: |
    **Номер**: `INT`, от 1 до 64 → номер бинарного выхода (не физического!)  
    **Значение**: `INT` → целое число, которое будет закодировано в биты согласно настройке `$BIN_OUT[номер]`
   example: |
    ```krl
    $B_OUT[3] = 7  ; (7 = 00111 в 5 битах)
    ```
    → KUKA установит:  
    - бит 3 = 1,  
    - бит 4 = 1,  
    - бит 5 = 1,  
    - бит 6 = 0,  
    - бит 7 = 0,  
    и вычислит бит чётности (например, в бит 8), чтобы общее число единиц было чётным.

 - name-var: $B_IN
   sintax: значение = $B_IN[номер]
   discriptions: |
      номер: INT, 1…64
      значение: INT — число, полученное из группы битов			
   example: |
    DECL INT: cmd
    cmd = $B_IN[1]

    SWITCH cmd
      CASE 1: ; запуск захвата
      CASE 2: ; смена инструмента
      ...
    ENDSWITCH

