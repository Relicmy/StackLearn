 - name-var: $CMD
   sintax: |
     CWRITE($CMD, "PTP XP1")
   discriptions: |
    Управляющий канал (handle) для выполнения команд в KRL
    🔹 Назначение
    $CMD — это специальный системный "канал", через который можно динамически отправлять команды KRL-интерпретатору во время выполнения программы, используя функцию CWRITE().

    Это аналог "консоли" или "потока команд" внутри самой KRL-программы. 

    🔹 Как это работает?
    Вы не присваиваете значение $CMD — она только для чтения (write-protected).
    Вы используете её как идентификатор канала в функции CWRITE()
    → Эта команда добавит движение PTP XP1 в буфер команд, и робот его выполнит после текущей инструкции (в порядке очереди).
    🔹 Тип данных
    $CMD имеет тип INT (целое число) — это уникальный номер (handle) командного канала.
    Но вам не нужно знать его числовое значение — просто используйте $CMD как есть.
    🔹 Когда это нужно?
    Динамическое формирование траекторий (например, на основе данных с датчиков).
    Исполнение команд, сгенерированных строкой (например, из файла или внешнего ПЛК).
    Отладка и тестирование — можно "вживую" вставлять команды без остановки программы.
    ⚠️ Важно: 

    Команды, отправленные через CWRITE($CMD, ...), выполняются асинхронно и в порядке очереди.
    Нельзя использовать CWRITE для обхода безопасности или блокировок.
    Не все команды допустимы — только выполняемые в движении (например, PTP, LIN, CIRC, но не DECL или DEF).
    🔹 Ограничения
    $CMD нельзя изменить — это системная константа.
    Функция CWRITE требует строку с корректной KRL-командой — ошибка в синтаксисе вызовет ошибку выполнения.
    Не подходит для критически важных в реальном времени участков — есть небольшая задержка на обработку.
    💡 Аналогия
    Представьте, что вы печатаете команды в командной строке, но не вручную, а из самой программы — вот что делает CWRITE($CMD, ...).

    Если вы работаете с динамическими траекториями или интеграцией с внешними системами — $CMD + CWRITE могут быть очень полезны.
   example: |
    ; Динамически отправляем команду движения
    CWRITE($CMD, "LIN XP2")

    ; Или с использованием переменной:
    DECL STRING cmd_str
    cmd_str = "PTP HOME"
    CWRITE($CMD, cmd_str)