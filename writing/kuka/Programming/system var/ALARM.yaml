 - name-var: $ALARM_STOP
   sintax: SIGNAL $ALARM_STOP $OUT[1013]
   discriptions: |
     🔴 Важно: это не обычные переменные, а сигналы (SIGNAL)
     Они автоматически привязаны к выходам ($OUT[...]) и управляются системой — ты не можешь писать в них напрямую.
     🔹 1. $ALARM_STOP — сигнал внешней аварийной остановки
     📌 Назначение:
     TRUE → нет аварийной остановки (всё в порядке).
     FALSE → сработала аварийная остановка (любого типа: с пульта или с внешнего устройства).
     💡 Практическое применение
     Диагностика на линии: понять, кто нажал EMG — оператор или сработала защита.
     Блокировка запуска: ПЛК не разрешает запуск, пока $ALARM_STOP = FALSE.
     Журнал событий: логировать типы остановок для анализа простоев.
   example: |
    $ALARM_STOP = FALSE и $ALARM_STOP_INTERN = TRUE 🚨 Сработал внешний EMG (через входы K1/K2 или безопасную шину

 - name-var: $ALARM_STOP_INTERN
   sintax: SIGNAL $ALARM_STOP_INTERN $OUT[1002]
   discriptions: |
     🔴 Важно: это не обычные переменные, а сигналы (SIGNAL)
     Они автоматически привязаны к выходам ($OUT[...]) и управляются системой — ты не можешь писать в них напрямую.
     🔹 1. $ALARM_STOP_INTERN — сигнал внутренней аварийной остановки
     📌 Назначение:
     TRUE → нет аварийной остановки (всё в порядке).
     FALSE → сработала аварийная остановка (любого типа: с пульта или с внешнего устройства).
     SIGNAL $ALARM_STOP $OUT[1013]
     💡 Практическое применение
     Диагностика на линии: понять, кто нажал EMG — оператор или сработала защита.
     Блокировка запуска: ПЛК не разрешает запуск, пока $ALARM_STOP = FALSE.
     Журнал событий: логировать типы остановок для анализа простоев.
   example: |
    $ALARM_STOP = FALSE и $ALARM_STOP_INTERN = FALSE ❗ ❗ Нажата кнопка EMG на SmartPAD