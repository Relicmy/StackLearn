 - name-var: $ANIN
   sintax: voltage = $ANIN[номер_входа]
   discriptions: |
     аналоговые входы (чтение напряжения)
     Номер входа: от 1 до 32 (максимум зависит от конфигурации I/O)
     📌 Назначение:
     Позволяет считывать напряжение с аналоговых входов (например, от датчиков давления, температуры, силы и т.д.).
     🔧 Тип:
     Только для чтения (write-protected)
     Возвращает значение в нормализованном диапазоне: от -1.0 до +1.0
     💡 Реальное напряжение зависит от модуля ввода и калибровки (scaling factor).
     Например: 
     -1.0 → -10 В
     0.0 → 0 В
     +1.0 → +10 В
     (но может быть и 0–10 В, 4–20 мА и др. — смотри настройки модуля в WorkVisual
   example: |
    DEF ReadForceSensor()
        DECL REAL norm_voltage
        DECL REAL force_N

        ; Читаем нормализованное напряжение с входа 1
        norm_voltage = $ANIN[1]

        ; Преобразуем в ньютоны (пример: 1.0 = 100 Н)
        force_N = norm_voltage * 100.0

        MsgNotify("Сила: %1 Н", force_N)
    END


 - name-var: $ANOUT
   sintax: $ANOUT[номер] = напряжение
   discriptions: |
     номер — целое число (INT) от 1 до 32
     → номер аналогового выхода (всего может быть до 32 выходов, в зависимости от конфигурации оборудования).
     напряжение — вещественное число (REAL) в диапазоне от –1.0 до +1.0
     → это нормализованное значение, а не реальные вольты. Например:
     0.0 — среднее положение (часто соответствует 0 В или 5 В в зависимости от модуля),
     1.0 — максимум (например, +10 В),
     –1.0 — минимум (например, –10 В или 0 В — зависит от типа модуля).
     Фактическое напряжение зависит от аппаратуры.
     Например:
     Модуль с диапазоном 0–10 В:
     –1.0 → 0 В, 0.0 → 5 В, +1.0 → 10 В
     (иногда –1.0 просто не поддерживается, и диапазон фактически 0…1.0 → 0…10 В).
     Модуль с диапазоном ±10 В:
     –1.0 → –10 В, 0.0 → 0 В, +1.0 → +10 В.
     Точное соответствие указано в документации на аналоговый I/O-модуль, установленный в вашей системе.
     Используется в реальном времени.
     Изменение $ANOUT[n] в программе сразу влияет на выходное напряжение (если выход сконфигурирован как аналоговый и активен).
   example: |
     ; Установить аналоговый выход 1 на 75% от максимума
     $ANOUT[1] = 0.75

     ; Установить выход 2 на "середину" (часто = 0 В или 5 В)
     $ANOUT[2] = 0.0