 - name-var: $AUX_POWER
   sintax: SIGNAL $AUX_POWER $IN[номер]
   discriptions: |
     это входной сигнал, который показывает наличие внешнего вспомогательного напряжения (обычно 24 В) на определённом входе контроллера.
     📌 Часто используется для подтверждения, что внешние устройства (например, клапаны, датчики, gripper) получают питание. 
     По умолчанию: $IN[1026]
     Тип: INT (номер входа)
     Вы можете изменить номер входа в машинных данных (например, если используете другую карту ввода). 

     🔧 Как это работает?
     Когда на физический вход (например, X11.2) подаётся 24 В от внешнего источника, KUKA интерпретирует это как $AUX_POWER = TRUE.
     Если напряжения нет — сигнал FALSE.
     📌 Это не питание самого робота, а вспомогательное питание для периферии, часто управляемое ПЛК. 
   example: |
    IF $IN[1026] == FALSE THEN
    HALT
    ; "Нет питания gripper'а!"
    ENDIF
    Диагностика: если gripper не работает — сначала проверяют $AUX_POWER.
    Блокировка движения:
    → Некоторые OEM-поставщики требуют, чтобы $AUX_POWER был активен, иначе робот не стартует.