 - name-var: $ACC
   sintax: |
     DECL ACCU_STATE bat_state
     bat_state = $ACCU_STATE
   discriptions: |
    Состояние батареи контроллера KUKA
    Предупреждение о риске потери данных: если батарея села, при отключении питания пропадут:
    калибровка робота (нулевые точки!),
    программы,
    настройки инструментов и баз.
    Профилактика: по значению можно понять, пора ли менять батарею.
    ⚠️ Если $ACCU_STATE == #VOLTAGE_LOW — немедленно сделай резервную копию и замени батарею! 
    📍 Где физически находится эта батарея?
    Обычно — на материнской плате KRC (KUKA Robot Controller),
    Тип: литиевая батарейка (например, CR2032 или аналог в промышленном исполнении),
    Меняется раз в 2–5 лет (зависит от температуры и условий).
    📌 Важно!
    $ACCU_STATE не относится к батарее робота-мобильной платформы (если у тебя AMR) — это только для контроллера KRC.
    Если у тебя KRC4 Compact или KRC5, батарея может быть интегрирована в ИБП — но логика мониторинга та же.
    ✅ Вывод:
    $ACCU_STATE — это "здоровье" резервной батареи контроллера.
    #OK
    Батарея в норме: напряжение и ток заряда в допустимых пределах
     #CHARGE_CURRENT_TOO_HIGH - Ток заряда слишком высокий (возможно, неисправность зарядной цепи)
     #CHARGE_CURRENT_TOO_LOW - Ток заряда слишком низкий (батарея изношена или отключена)
     #VOLTAGE_LOW Напряжение батареи ниже критического уровня → данные могут быть потеряны при отключении питания!
     #NOT_AVAILABLE Система не может определить состояние (например, батарея не установлена или не поддерживается)

   example: |
    DEF CheckBatteryOnStartup()
      DECL ACCU_STATE bat_state

      bat_state = $ACCU_STATE

      IF bat_state == #VOLTAGE_LOW THEN
          MsgNotify("ВНИМАНИЕ: Низкое напряжение батареи! Сделайте резервную копию.")
      ELSEIF bat_state <> #OK THEN
          MsgNotify("Батарея: %1", bat_state)
      ENDIF
     END