 - name-var: $ACT_ADVANCE
   sintax: |
    DECL INT planned_blocks
    planned_blocks = $ACT_ADVANCE
   discriptions: |
    📌 Назначение:
     Показывает, сколько команд движения (LIN, PTP и т.д.) уже спланировано в буфере Advance Run, но ещё не выполнено.
    🔧 Тип: INT
    Только для чтения (write-protected)
    Диапазон: 0 … $ADVANCE + 1
    💡 Advance Run — это механизм KUKA, при котором контроллер заглядывает вперёд по программе (на $ADVANCE блоков), чтобы плавно связывать движения.
    📌 Когда это полезно?
    Отладка логики синхронизации,
    Понимание, почему робот не останавливается мгновенно (потому что уже запланированы следующие движения),
    При работе с внешним управлением (RSI) — нужно знать, сколько «в запасе».
   example: |
    DECL INT planned_blocks
    planned_blocks = $ACT_ADVANCE
    MsgNotify("Запланировано движений: %1", planned_blocks)

 - name-var: $ACT_BASE
   sintax: $ACT_BASE = 3
   discriptions: |
    📌 Назначение:
    Показывает, какая система координат BASE будет использоваться в следующих движениях (в режиме предварительного просмотра).
    🔧 Тип: INT
    Записываемая (можно менять в программе)
    После сброса программы = -1
    💡 Это аналог команды BASE = BAS(#BASE3), но через системную переменную. 
   example: |
    ; Переключаемся на BASE 3
    $ACT_BASE = 3
    LIN XP1   ; XP1 будет интерпретироваться в системе BASE 3

 - name-var: $ACT_BASE_C
   sintax: current_base = $ACT_BASE_C
   discriptions: |
    📌 Назначение:
    Показывает, какая BASE реально используется в текущем (выполняемом) движении.
    🔧 Тип: INT
    Только для чтения
    📌 Используй $ACT_BASE_C, если нужно программно определить, в какой системе координат сейчас работает робот. 
   example: |
    DECL INT current_base
    current_base = $ACT_BASE_C
    MsgNotify("Сейчас используется BASE: %1", current_base)

 - name-var: $ACT_TOOL
   sintax: $ACT_BASE = 3
   discriptions: |
    Номер текущего TOOL в Advance Run
    📌 Назначение:
    Задаёт инструмент, который будет использоваться в следующих движениях.
    🔧 Тип: INT
    Записываемая
    После сброса = -1
    💡 Эквивалент: TOOL = TOOL_DATA[2] 
   example: |
    ; Использовать TOOL 2 (например, сварочная горелка)
    $ACT_TOOL = 2
    LIN XP1   ; движение с учётом смещения и массы TOOL 2

 - name-var: $ACT_TOOL_C
   sintax: current_tool = $ACT_TOOL_C
   discriptions: |
    📌 Назначение:
    Показывает, какой инструмент реально используется в текущем движении.
    🔧 Тип: INT
    Только для чтения
   example: |
    DECL INT current_tool
    current_tool = $ACT_TOOL_C
    IF current_tool == 1 THEN
        MsgNotify("Работаем захватом")
    ELSEIF current_tool == 2 THEN
        MsgNotify("Работаем клеевым пистолетом")
    ENDIF